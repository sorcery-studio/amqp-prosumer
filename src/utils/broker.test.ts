describe("Broker Utils", () => {
  describe("Connecting to broker", () => {
    test.todo("Connection can be established");
  });

  describe("Disconnecting from broker", () => {
    test.todo("Connection can be closed");
  });
});
